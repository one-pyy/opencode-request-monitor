[project]
name = "opencode-request-monitor"
version = "0.1.0"
description = "OpenCode request monitor with FastAPI and SQLite"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.115.0",
  "httpx>=0.28.0",
  "uvicorn>=0.30.0",
]

[tool.uv]
package = false
